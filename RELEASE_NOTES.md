# OJ Insight Release Notes

## v0.8.0 - 2026-09-11

v0.8.0 开始加入 XCPC Tracker，在不改变现有平台统计页和设置结构的前提下，将 ICPC、CCPC 与省赛补题整理为统一比赛矩阵。

### 新增与调整

- 侧边栏新增 XCPC Tracker 入口，复用现有主题、字号、密度和本地偏好体系。
- 比赛按时间倒序展示，支持系列、阶段、年份、赛站、完成进度与关键词筛选。
- 比赛、日期和进度固定在左侧；仅题目区域参与横向滚动。
- 默认隐藏题名并在一屏展示 A–M，开启题名后显示 `A. 题目名称` 与正式队通过数。
- 支持比赛全称和简称切换，以及榜单难度颜色开关。
- AC 使用绿色背景，金银铜铁只控制题目文字颜色；悬停整行不会覆盖 AC 状态。

### 发布

- 应用版本统一为 0.8.0。
- GitHub Actions 普通手动运行可生成三平台测试包；输入 `v0.8.0` 或推送同名标签可准备签名 Draft Release。

## v0.7.1 - 2026-09-10

v0.7.1 聚焦日常使用体验，在不改变既有页面布局和组件设计的前提下，增加灰色主题、完整难度覆盖、启动自动同步和应用内安全更新。

### 新增与调整

- 个性化设置新增灰色主题，保留当前绿色强调色和各 OJ 原有颜色语义。
- 难度分布加入“未评级”，不再隐藏已经识别但缺少可靠难度的题目；难度砖使用独立纹理显示只有未评级题目的日期。
- 启动后先展示本地缓存，再后台增量同步全部已配置平台；设置中可关闭。
- 启动后后台检查更新；发现新版时可以立即下载、验签、安装，也可以稍后处理或跳过当前版本。
- 面向新用户继续只发布 EXE、Universal DMG 和 AppImage。
- 修复数据源页面“同步全部”按钮以及已有 Draft Release 时的发布流程。
- 三端安装包使用明确的平台后缀，自动更新清单与 Release 中的实际文件名保持一致。

## v0.6.0 - 2026-09-08

v0.6.0 在保持 v0.5.1 既有页面、卡片和视觉风格的基础上，补齐 Rating 交互、难度逐题查看、个人信息导出与三平台发布流程。

### 新增与调整

- Rating 曲线点悬停显示比赛名称、日期、排名及 Rating 变化，点击可打开 Codeforces、AtCoder 或 LeetCode 对应比赛；同时支持键盘聚焦与回车跳转。
- Rating 用户名放大至接近当前 Rating 数字的视觉层级。
- 点击难度分布柱形可打开侧边抽屉，查看该档全部去重题目并跳转题面；数据源只有汇总数据时会明确显示可用边界。
- 设置的账号页新增个人信息 JSON 导出。安全导出默认不含 Cookie / Session；完整导出前会显示敏感信息警告。主题、字号、时区等个性化设置以及训练记录均不导出。
- 导出页使用原有切换样式整合活动砖图片和个人信息导出。
- 关于页 `OI` 标识修正为 `OJI`。

### MacOS 与发布

- MacOS Universal 构建增加临时签名验证。未配置 Apple Developer ID 与公证时，Gatekeeper 仍可能给可信开源包加上隔离标记，处理方式见 `docs/BUILDING.md`。
- 三个平台完成后直接生成 `OJ-Insight-All-Platforms.zip`，根目录只包含 `.exe`、`.dmg` 与 `.AppImage` 各一个。
- 发布显示名称统一使用 `MacOS`，版本统一更新为 `0.6.0`。

## v0.5.1 - 2026-09-07

v0.5.1 是一次以稳定性、布局适配和视觉细节为主的维护版本，保留原有页面结构与使用方式。

### 修复

- 修复字号调整后难度分布柱子错位、标签遮挡和部分内容不可见的问题。
- 修复难度分布中不同 OJ 柱子粗细不一致、整体不居中的问题。
- 修复难度分布内部背景出现绿色渐变、无法与卡片背景融合的问题。
- 修复 AtCoder 难度分布读取区间错误导致颜色异常的问题。
- 修复洛谷同步无法获取具体提交时被错误计入 OJ 提交统计的问题。
- 修复亮色模式下关于页面 OI 标志、数据源清空区域和导出示例背景显示为黑色的问题。
- 修复左上角 Logo 被拉伸的问题。

### 界面改进

- 难度分布柱状图根据当前 OJ 的难度档位数量动态调整间距和宽度。
- 难度分布横向铺满可用区域，仅在左右保留必要空间；柱宽根据档位数量动态调整。
- 同一 OJ 内的柱子保持等宽，整体自动居中，并为解题数量保留独立显示空间。
- 解题数量显示在对应柱子上方，并保持适当间距。
- Codeforces、AtCoder、洛谷和 LeetCode 难度颜色分别参考对应 OJ 的原有色彩体系。
- 活动砖改为使用同一基础色的透明度层级表达活跃程度。
- 提高亮色模式下活动砖的辨识度。
- Logo 固定为正方形圆角容器。
- Rating 区域同时突出当前 Rating 与历史最高 Rating。
- Rating 用户名使用当前段位对应颜色，并优化近期变化、近 90 天净变化和 Rated 场次数据展示。
- 放大 Rating 总览中的用户名。
- 提高亮色模式下 AtCoder 灰色标识和最低难度柱的对比度。
- 移除侧边栏中重复的 Issue 反馈入口，统一保留关于页面入口。
- 移除界面中的 Local-first 提示文字。
- 隐藏侧边栏滚动条视觉样式，同时保留正常滚动能力。

### 数据同步

- 洛谷无法取得逐条提交但成功取得公开活动或题目统计时，直接显示为同步成功。
- 洛谷的活动统计不会被误认为逐条提交数据。
- 同步结果增加部分成功状态提示。

### macOS 与构建

- macOS 构建调整为 Universal App，同时支持 Intel 与 Apple Silicon。
- 增加 macOS 最低系统版本配置和 Universal 构建检查。
- 统一前端、Rust 和 Tauri 配置中的版本号为 `0.5.1`。
- 增加版本一致性检查脚本。

### 项目结构

- 将设置、数据源、导出和关于页面从原 `UtilityPages.tsx` 拆分为独立页面文件。
- 将旧版构建说明整理为 `docs/BUILDING.md`。
- 清理不再使用的 Tauri 自动生成 schema 文件。
- 更新 `README.md`、`CHANGELOG.md` 和构建工作流说明。

### 已知事项

- 比赛日历、比赛提醒、比赛数据包导出、自定义主题导入等功能暂未包含在本版本中。
- 完整桌面安装包仍需要在具备 Node.js、Rust 和对应平台构建环境的机器上生成。
